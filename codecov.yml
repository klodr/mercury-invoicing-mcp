coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
        only_pulls: false
        if_no_uploads: success
        if_not_found: success
    patch:
      default:
        target: 95%
        threshold: 1.5%
        only_pulls: false
        if_no_uploads: success
        if_not_found: success

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: false
